<% if flash[:notice] %>
    <%= flash[:notice] %>
<% end %>

<% @tweets.each do |e| %>
    <p>
        <%= e.message %>
        <%= ((Time.current - e.tdate)/60).to_i %>分
        <%=link_to '削除', tweet_path(e), data: { turbo_method: :delete } %>
        <%=link_to '表示', tweet_path(e) %>
        <%=link_to '編集', edit_tweet_path(e) %>
    </p>
    <p>&#x266f;　　　&#x21BB;　　　&#x2661;</p>
<% end %>

<%= link_to '新規追加', 'tweets/new' %>

